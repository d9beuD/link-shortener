{{ form_start(form, { form_class: 'mb-3' }) }}
    {{ form_row(form.destinationUrl, {
        label: 'Put your link and let the magic happen.',
        attr: { placeholder: 'https://...', },
        label_class: 'text-slate-600 dark:text-slate-300',
        row_class: 'mb-4',
        widget_class: 'w-full rounded-lg border-slate-300 dark:bg-slate-950 dark:border-slate-600 dark:text-slate-200 dark:placeholder:text-slate-600',
    }) }}
    <div class="flex justify-end">
        <button class="bg-purple-600 text-white px-4 py-2 rounded-lg">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
